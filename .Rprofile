if (interactive()) {
  message("Remember to run devtools::check using cimentadaj::adapted_check because it sets the NOT_CRAN variable to 'true' to run the skipped tests. Moreover, remember to set the variable Sys.setenv('ess_email' = 'cimentadaj@gmail.com') before doing doing the check")
}


